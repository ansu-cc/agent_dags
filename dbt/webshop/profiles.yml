ai_lab:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: "{{env_var('AIRFLOW_POSTGRES_USER')}}"
      password: "{{env_var('AIRFLOW_POSTGRES_PASSWORD')}}"
      port: 5432
      dbname: webshop_v2
      schema: webshop  # This ensures that all dbt objects go into webshop_v2
      threads: 6

#working version
